# Per-post view counts. Any Upstash-compatible Redis REST endpoint; the Vercel
# Marketplace "Redis" (Upstash) sets these two automatically. Leave them unset
# and the counter simply does not render.
KV_REST_API_URL=
KV_REST_API_TOKEN=
