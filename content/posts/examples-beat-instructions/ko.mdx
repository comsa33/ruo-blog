---
title: '예시가 지시문을 이긴다'
description: '한국어 대화에서 뽑은 기억이 영어로 저장되고 있었다. 지시문을 어디에 넣어도 소용없었고, 원인은 다른 곳에 있었다.'
topic: '에이전트 메모리'
date: 2026-08-15
tags: ['프롬프트 설계', 'LLM', '한국어']
---

에이전트에 장기 기억을 붙이는 일을 맡았다. 사용자가 대화 중에 흘린 사실을 추출해 저장해 두고, 다음 대화에서 꺼내 쓰는 구조다.

어느 날 저장된 기억을 열어 봤더니 이렇게 적혀 있었다.

> User asked why pgvector 0.8 is necessary for service deployment.

대화는 처음부터 끝까지 한국어였다.

## 이 일이 어디서 일어나는가

먼저 구조부터. 기억 서비스는 대화 원문을 소유하지 않는다. 원문은 대화로그 저장소 한 곳에만 있고, 이쪽은 **요약과 커서와 기억**만 가진다.

<Structure
  nodes={[
    { id: 'user', label: '사용자', col: 0, row: 0 },
    { id: 'agent', label: '에이전트', col: 1, row: 0, note: '매 턴 프롬프트를 조립한다. 기억은 여기로 주입된다.' },
    { id: 'logs', label: '대화로그 저장소', sub: 'source of truth', col: 2, row: 0, note: '대화 원문의 유일한 원본. 기억 서비스는 이것을 복제하지 않고 필요할 때 다시 읽는다.' },
    { id: 'queue', label: '신호 큐', sub: 'stream', col: 1, row: 1, note: '"새 턴이 생겼다"는 사실만 전달한다. 대화 내용은 싣지 않는다.' },
    { id: 'worker', label: '적재 워커', col: 2, row: 1, accent: true, note: '이 글의 무대. 대화를 읽어 기억으로 만드는 곳이고, 추출 프롬프트가 도는 자리다.' },
    { id: 'store', label: '기억 저장소', sub: 'vector + ledger', col: 3, row: 1, note: '추출된 기억과 그 출처만 남는다. 원문은 없다.' },
  ]}
  edges={[
    { from: 'user', to: 'agent' },
    { from: 'agent', to: 'logs', label: '원문 기록' },
    { from: 'agent', to: 'queue', label: '신호', dashed: true },
    { from: 'queue', to: 'worker' },
    { from: 'worker', to: 'logs', label: '다시 읽기' },
    { from: 'worker', to: 'store', label: '추출·저장' },
    { from: 'store', to: 'agent', label: '회상' },
  ]}
  hint="노드에 커서를 올리면 연결과 설명이 나타납니다."
/>

원문을 복제하지 않기로 한 것은 초기 결정 중 가장 잘한 것이었다. 보존 기간, 삭제 요청, 개인정보 범위가 전부 저장소 한 곳의 문제로 남는다. 대신 워커가 매번 원문을 다시 읽어야 한다.

## 매 턴마다 추출하면 안 되는 이유

처음 설계는 단순했다. 턴이 하나 생기면 그 턴을 추출한다.

토큰을 재보고 접었다.

<Breakdown
  caption="추출 1회 입력의 구성"
  unit=" 토큰"
  segments={[
    { label: '고정 추출 프롬프트', value: 7606, note: '대화가 무엇이든 매번 똑같이 들어가는 부분. 전체의 80%다.' },
    { label: '직전 대화 5턴', value: 965, note: '문맥 유지용. 대화 길이와 무관하게 상한이 있다.' },
    { label: '기존 기억 20건', value: 800, note: '중복 저장을 막으려고 함께 넣는다.' },
    { label: '실제 처리할 대화 1턴', value: 193, note: '정작 이번에 새로 들어온 내용. 전체의 2%다.' },
  ]}
  hint="막대나 항목에 커서를 올리면 설명이 나옵니다."
/>

2%를 처리하려고 98%를 매번 다시 보냈다. 고정비가 지배하는 구조에서 답은 하나다. **묶어서 고정비를 나눠 문다.**

<Playground
  caption="묶음 크기에 따른 1,000턴 처리 비용"
  param={{ label: '한 번에 묶는 턴 수', initial: 5, unit: '턴' }}
  rows={Array.from({ length: 20 }, (_, i) => {
    const n = i + 1;
    const batches = Math.ceil(1000 / n);
    const total = (7606 + 965 + 800 + 193 * n) * batches;
    const base = (7606 + 965 + 800 + 193) * 1000;
    return {
      value: n,
      results: [
        { label: '추출 호출 횟수', value: batches.toLocaleString() },
        { label: '총 입력 토큰', value: (total / 1000000).toFixed(2) + 'M' },
        { label: '매 턴 대비', value: (base / total).toFixed(1) + '배 절감', highlight: true },
      ],
    };
  })}
/>

5턴으로 묶으면 4.6배가 줄어든다. 슬라이더를 더 밀면 더 줄지만 수확은 빠르게 체감한다. 고정비를 나눠 무는 효과는 초반에 다 나오고, 그 뒤로는 기억이 저장되기까지의 지연만 길어진다. 실서비스에서 기억이 필요해지는 시점도 세션 단위라 5턴 언저리가 실제와 가장 가까웠다.

<Sequence
  caption="적재 흐름"
  actors={['에이전트', '신호 큐', '적재 워커', '대화로그', '기억 저장소']}
  steps={[
    { from: '에이전트', to: '신호 큐', label: '턴 생겼음', async: true, note: '매 턴 신호를 보내지만 대화 내용은 싣지 않는다. 큐는 "무언가 생겼다"만 알린다.' },
    { from: '신호 큐', to: '적재 워커', label: '깨움', note: '워커는 커서에 미처리 턴 수를 올리고, 임계에 못 미치면 아무것도 하지 않는다.' },
    { from: '적재 워커', to: '적재 워커', label: 'N턴 or 무활동 대기', note: 'N턴이 쌓이거나 일정 시간 조용해지면 그때 움직인다. 이 대기가 고정비를 나눠 무는 장치다.' },
    { from: '적재 워커', to: '대화로그', label: '커서 이후 조회', note: '큐가 실어온 내용을 믿지 않고 원본을 다시 읽는다. 워커가 죽었다 살아나도 같은 자리에서 재개된다.' },
    { from: '대화로그', to: '적재 워커', label: '원문 반환', note: '상한을 넘으면 잘라낸다. 커서 없는 방의 첫 적재가 수백 턴을 들고 오는 것을 막는다.' },
    { from: '적재 워커', to: '적재 워커', label: '마스킹 후 추출', note: '개인정보를 지운 뒤에만 모델에 넣는다. 마스킹이 실패하면 원문으로 넘어가지 않고 그냥 실패시킨다.' },
    { from: '적재 워커', to: '기억 저장소', label: '기억 저장', note: '저장이 끝나야 커서를 옮긴다. 순서가 반대면 실패한 구간이 조용히 사라진다.' },
  ]}
/>

## 지시문을 어디에 넣어도 안 됐다

고정비가 80%라면 묶는 것 말고 다른 길도 있다. **고정비 자체를 줄이는 것.** 그 프롬프트를 열어 보다가 언어 문제와 마주쳤다.

먼저 한 일은 시키는 것이었다. "입력과 같은 언어로 써라." 네 가지 조건으로 실험했다.

| 조건 | 지시문 위치 | 결과 |
|---|---|---|
| A | 유저 프롬프트 끝에 8줄 | 영어 |
| B | 시스템 프롬프트 맨 뒤에 강하게 | 영어 |
| C | 시스템 프롬프트 맨 앞에 강하게 | 영어 |
| E | 예시 없는 최소 프롬프트 + 한 줄 지시 | **한국어** |

A·B·C가 전부 실패하고 E만 통과했다는 것이 답이었다. 위치의 문제가 아니었다. E에서 뺀 것은 위치가 아니라 **예시**였다.

## 원인은 예시 14개였다

프롬프트는 33,661자였고, 그 안에 영어 few-shot 예시가 14개 있었다. 그리고 그 프롬프트 전체에서 `language`라는 단어는 **0번** 나왔다. 한글도 한 글자 없었다.

모델은 지시문을 안 읽은 게 아니다. 읽었지만, 열네 번 반복된 "한국어를 받아 영어로 쓰는" 시연이 한 줄짜리 지시를 이겼다.

<Callout label="추가로 알게 된 것" accent>
쓰고 있던 오픈소스 메모리 엔진의 이전 버전에는 `record the facts in the same language`가 프롬프트 본문에 박혀 있었다. 다음 버전이 그 문장을 옵션으로 빼면서 기본 동작에서 사라졌다. 업그레이드 노트에는 없던 변화였다.
</Callout>

## 같은 입력, 다른 프롬프트

예시를 3개로 줄이고 그중 하나를 한국어 입출력 쌍으로 두었다. 언어 규칙은 문장이 아니라 **예시로** 뒷받침했다.

<Transform
  caption="프롬프트만 바꾸고 입력은 그대로"
  inputLabel="입력 — 한국어 대화"
  input={[
    { speaker: 'user', text: '우리 서비스 배포할 때 pgvector 0.8 이 꼭 필요한 이유가 뭐였지?' },
    { speaker: 'assistant', text: 'HNSW 가 post-filter 라서 스코프 필터가 강하면 결과가 모자랍니다. 0.8 의 iterative index scan 이 그걸 해결합니다.' },
  ]}
  outputLabel="추출된 기억"
  variants={[
    {
      key: 'old',
      label: '기존 · 33,661자 · 영어 예시 14개',
      output: [{ text: 'User asked why pgvector 0.8 is necessary for service deployment.' }],
      verdict: '한국어 대화인데 영어로 저장됐다. 게다가 assistant 가 말한 기술적 사실은 통째로 빠졌다.',
      bad: true,
    },
    {
      key: 'new',
      label: '축소 · 3,549자 · 한국어 예시 포함',
      output: [
        { attr: 'user', text: '사용자는 서비스 배포에 pgvector 0.8 이 필요한 이유를 확인했다.' },
        { attr: 'assistant', text: 'HNSW 는 post-filter 방식이라 스코프 필터가 강하면 결과 수가 모자라는데, pgvector 0.8 의 iterative index scan 이 이 문제를 해결한다.' },
      ],
      verdict: '언어가 유지되고, 누가 말한 사실인지도 나뉘어 붙었다.',
    },
  ]}
/>

## 실측

합성 데이터로 두 번 재고, 세 번째는 실제 대화 6개 스레드로 쟀다. 순서 편향을 없애려고 A/B를 뒤집어 두 번씩 돌렸다.

<Compare
  caption="프롬프트 길이"
  beforeLabel="기존"
  afterLabel="축소"
  before={33661}
  after={3549}
  beforeText="33,661자"
  afterText="3,549자"
  note="<strong>1/9.5</strong>로 줄었다. 출력 스키마는 그대로 두고 가이드라인과 예시만 압축했다."
/>

<Compare
  caption="추출 1회 입력 토큰 (실제 대화 6스레드 평균)"
  beforeLabel="기존"
  afterLabel="축소"
  before={12810}
  after={6112}
  beforeText="12,810"
  afterText="6,112"
  note="회당 <strong>6,698 토큰</strong> 절감. 비율로는 52.3%인데, 3차 데이터는 대화 본문 자체가 길어서 프롬프트 비중이 낮게 나왔다."
/>

<Metrics>
  <Metric value="50% → 100%" label="한국어 유지율 (3/6 → 6/6)" />
  <Metric value="9 : 3" label="품질 판정, 축소판 승" />
  <Metric value="97 → 69" label="추출 건수. 과분할이 줄어든 것으로 본다" />
</Metrics>

가장 뼈아픈 숫자는 50%다. 그전까지 알고 있던 영어 추출률은 13.4%였다. 합성 데이터로 잰 값이었고, 실제 대화에서는 네 배 가까이 나빴다.

## 덤으로 따라온 것

예시를 다시 쓰면서 그동안 방치돼 있던 오염 두 가지도 같이 규칙에 넣었다.

하나는 **회상 재적재**다. 사용자가 "내가 뭘 좋아한다고 했지?"라고 묻고 에이전트가 저장된 기억을 나열하면, 그 나열이 새 사실로 다시 추출됐다. 같은 사실의 사본이 대화할 때마다 불어났다.

다른 하나는 **부재 진술**이다. "그런 기록은 없네요"라는 답변이 "기록이 없다"는 기억으로 저장됐다. 조회 결과는 기억이 아니다.

| 오염 | 기존 | 축소 |
|---|---|---|
| 회상 재적재 | 2/7 | 0/7 |
| 부재 진술 | 5/7 | 0/7 |
| 귀속 정확도 | 회귀 없음 | 회귀 없음 |

사본이 실제로 얼마나 불어나는지는 세션을 쌓아 봐야 보인다. 열다섯 세션을 연속으로 돌리며 누적 중복 사본을 셌다.

<Series
  caption="세션이 쌓일수록 누적되는 중복 사본"
  xLabel="세션"
  yLabel="누적"
  series={[
    { name: '억제 없음', values: [0, 1, 4, 6, 6, 9, 11, 12, 14, 19, 20, 22, 25, 30, 33] },
    { name: '1차 억제', values: [4, 7, 10, 11, 11, 11, 12, 13, 13, 16, 16, 17, 17, 17, 17] },
    { name: '작은 모델 참조군', values: [0, 0, 0, 1, 1, 1, 3, 3, 5, 7, 7, 7, 7, 7, 7] },
    { name: '최종', values: [1, 1, 1, 1, 2, 2, 3, 3, 3, 3, 3, 3, 3, 3, 3], accent: true },
  ]}
/>

억제가 없으면 사본이 선형으로 늘어난다. 중요한 것은 최종선이 낮다는 것보다 **일찍 평평해진다**는 점이다. 같은 사실을 다시 말해도 새 사본이 생기지 않는다는 뜻이다. 이 사이 정당한 사실의 보존은 24건 중 22건으로 유지됐다.

## 못 고친 것

프롬프트로 안 되는 것도 확실해졌다.

에이전트가 "남색이 무난합니다"라고 제안하고 사용자가 아무 대답도 하지 않았는데, "사용자는 남색을 선호한다"가 저장되는 경우다. 지시문으로도, 예시를 더해도, 무응답 예시를 따로 넣어도 3번 중 3번 다 실패했다.

없는 결정을 지어내는 쪽은 규칙으로 전부 막혔다. 하지만 **말해진 것을 누구 의견으로 볼지**는 프롬프트가 판정할 문제가 아니었다. 이건 적재 이후의 필터 계층에서 다뤄야 한다.

## 남은 것

세 가지를 배웠다.

**모델에게 예시는 지시보다 강한 신호다.** 프롬프트에 규칙과 시연이 함께 있고 둘이 어긋나면, 이기는 쪽은 시연이다. 그러니 지시문을 강화하는 방향으로 고치려 들면 계속 실패한다. 어긋난 시연을 찾아 고쳐야 한다.

**합성 벤치는 스케일의 자리만 정해 준다.** 13.4%와 50%의 차이가 그 값이다. 합성 데이터로 임계값의 자릿수를 잡는 것까지는 유효하고, 그 값을 실환경 근거로 쓰는 순간 틀린다.

**긴 프롬프트는 비용이자 버그 표면이다.** 33,661자 안에 `language`가 0번 나온다는 사실을 아무도 몰랐다. 줄이는 작업이 곧 읽는 작업이었고, 문제는 읽는 중에 발견됐다.
